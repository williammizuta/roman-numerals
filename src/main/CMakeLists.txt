SET(SourceFiles
  main.cpp
)

INCLUDE_DIRECTORIES(
  ${CMAKE_CURRENT_BINARY_DIR}
  ${CMAKE_CURRENT_SOURCE_DIR}
)

ADD_EXECUTABLE(roman ${SourceFiles})

INSTALL(TARGETS roman
  RUNTIME DESTINATION bin
  BUNDLE DESTINATION bin
)
